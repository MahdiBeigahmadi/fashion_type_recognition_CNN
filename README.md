# fashion_type_recognition_CNN

It is the same project as https://github.com/MahdiBeigahmadi/multiclass_classification_FashionDetection
but it uses convolutional neural network for model generation. 
